data = {...printEntry}

vars = {
    periodeList: [
        {periode: 'Harian'},
        {periode: 'Bulanan'},
        {periode: 'Tahunan'},
    ],
}

components = {
    datepicker: DatePicker,
	vueselect: VueSelect.VueSelect,
}